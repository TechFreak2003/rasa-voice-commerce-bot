version: "3.1"
rules:
  - rule: Add item to cart
    steps:
      - intent: add_to_cart
      - action: action_add_to_cart

  - rule: Proceed to checkout
    steps:
      - intent: checkout
      - action: action_checkout
